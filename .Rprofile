# Ce fichier s'exécute au démarrage de R
# Il installe les dépendances nécessaires, notamment shinymanager depuis GitHub

if (Sys.getenv("RSTUDIO") == "1" && file.exists("requirements.R")) {
  source("requirements.R")
}
